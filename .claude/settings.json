{
  "permissions": {
    "allow": [
      "Bash(curl -s http://localhost:8765)",
      "Bash(pkill -f \"streamlit run app.py\")",
      "Bash(pkill -f \"server.port 8765\")",
      "Bash(pkill -f \"server.port 8782\")"
    ]
  }
}
